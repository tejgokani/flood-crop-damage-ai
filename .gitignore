# Environments
.venv/
.uvcache/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Dataset cache, fetched on demand -- never committed.
# Anchored with a leading slash: an unanchored 'data/' would also swallow src/fcda/data/.
/data/
!/data/.gitkeep

# Training artefacts
checkpoints/
*.pt
*.pth

# Report figures ARE committed: the README embeds them, so a fresh clone must render.
# They are small PNGs regenerated by `make report`.

# OS
.DS_Store

# PowerPoint lock files
~$*

# Sen1Floods11 chips (135 MB, fetched on demand)
/data/sen1floods11/
